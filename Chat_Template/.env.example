# ============================================
# PUBLIC ENVIRONMENT VARIABLES (exposed to browser)
# ============================================
# Only WebSocket URL should be public since browser connects directly to Rust WebSocket
NEXT_PUBLIC_CHAT_WS_URL=ws://localhost:8085

# ============================================
# SERVER-SIDE ONLY ENVIRONMENT VARIABLES (NOT exposed to browser)
# ============================================
# These are used by Next.js API routes for server-to-server communication

# Auth service URL (for login/register via Next.js API routes)
AUTH_API_URL=http://localhost:8081

# Core service URL (for user management via Next.js API routes)
CORE_API_URL=http://localhost:8082

# Chat service URL (for chat API calls via HAProxy internal port)
# Port 8086: Internal API with round-robin load balancing (used by Next.js server)
# Port 8085: Public WebSocket with sticky sessions (used by browser)
CHAT_SERVICE_URL=http://localhost:8086

# API Key for auth service (server-side only)
# Get this key from your Rust auth-service configuration
AUTH_API_KEY=dev-key-12345
